# 攻略截图来源记录

采集日期：2026-09-16。

这些图片来自本地运行的恢复版 Unity WebGL Development Build，不是 AI 生成图，也不是完整通关证明。原始 PNG 保存在 `captures/`；网页使用裁掉画布外黑边后的 960×600 WebP。

| WebP | 真实采集内容 | 正文用途 |
| --- | --- | --- |
| `s00-web-menu.webp` | Demo 2.0 菜单 | Walkthrough 版本入口 |
| `s02-inspection-tutorial.webp` | Helper.exe 的眼睛、手与背包图标教程 | 操作说明 |
| `s03-office-start.webp` | 教程结束后的办公室起点 | 办公室章节环境 |

转换使用 `D:\03_website\00-convert-tool\framework-qrx-next` 已安装的 FFmpeg WASM 内核，编码为 `libwebp -q:v 86`。可通过项目 `scripts/convert-guide-images.mjs` 复现。

未采集到的听筒、电话、转盘、监狱门等关键画面不在此清单中，不得以现有图片替代并宣称已验证。
